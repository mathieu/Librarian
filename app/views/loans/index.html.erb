<% content_for :title, 'All Loans' %>
<% content_for :navbar_link, 'loans'%>



<div class="page-title">
  <h1>Loans</h1>
</div>

<div class="row col-md-11">
	<table>
		<tr><th>Title</th><th>User</th><th>Action</th>
	  <% Loan.where(returned: false).paginate(:page => params[:page]).each do |loan| %> 
	  	<%= render partial: 'loans/line', locals: {loan: loan} %>
		<% end %>
	</table>
</div>
